name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Test project
        run: echo "Testing HTML/CSS files"

      - name: Build Docker image
        run: docker build -t monsite .